database:
  image: tutum/mysql
  ports:
    - "3306"
  environment:
    MYSQL_USER: admin
    MYSQL_PASS: password
trendio:
  build: .
  command: start
  ports:
    - "2345:2345"
  volumes:
    - .:/trendio
  links:
    - database
