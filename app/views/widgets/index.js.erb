$('#container').append("<%=j render(partial: 'layouts/feed', format: 'html') %>");

<% if @widgets.current_page == @widgets.total_pages %>
  $('#view-more').remove();
<% else %>
  $('#view-more a').attr('href', '<%= url_for(page: @widgets.current_page + 1) %>');
<% end %>